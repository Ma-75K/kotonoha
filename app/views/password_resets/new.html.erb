<h1>パスワード再設定</h1>

<%= form_with url: password_reset_path, method: :post, local: true do %>
  <div>
    <%= label_tag :email, "メールアドレス" %>
    <%= email_field_tag :email, @email, placeholder: "kotonoha@example.com" %>
  </div>

  <div>
    <%= submit_tag "送信する" %>
  </div>
<% end %>
